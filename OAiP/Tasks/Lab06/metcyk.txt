

                  ОПЕРАТОРЫ ОРГАНИЗАЦИИ ЦИКЛОВ


           Циклы организуются для того, чтобы повторить некоторую
     инструкцию или группу инструкций определенное число раз. В языке СИ
     существует три оператора организации циклов; for(для,в течение),
     while(пока) и do-while(делать-пока).
          Первый из них формально записывается так:
             For (выражение 1; выражение 2; выражение 3) тело цикла

     Тело цикла составляет либо одна инструкция, либо любое подмножество
     инструкций, заключенных в фигурные скобки.
     В выражениях 1,2,3 фигурирует специальная переменная, называемая уп-
     равляющей. По ее значению устанавливается необходимость повторения
     цикла либо выхода из него.(см.пример 1). Выражения 1 и 3 могут
     содержать в себе несколько выражений разделяемых запятыми(см. пример 2).

          Каждое из этих выражений в скобках используется: выражение 1 -
     - для присвоения начального значения управляющей переменной; выраже-
     неие 2 - для проверки выполнения условия продолжения цикла; выраже-
     ние 3 -  для изменения управляющей переменной.
          Таким образом, выражение 1 присваивает начальное значение управ-
     ляющей переменной, выражение 3 изменяет его на каждом шаге, а выраже-
     ние 2 проверяет, достигло ли оно граничного значения, устанавливающего
     необходимость выхода из цикла.
         Любое выражение 1,2,3 может отсутствовать, но точка с запятой оста-
     ется.
        Пример 1:
           ......................
           for(i=1;i<=k;i++)
            {
              j=i+3;
              printf("%d ",j);
             }
            .....................

        Пример 2:
            .....................
            for(i=0,k=10;i<k;i+=3)
              {
               printf("%d\n",i);
              }




      Допускаются вложенные конструкции,т.е. в теле некоторого цикла могут
      встречаться другие операторы for.

           Оператор while формально записывается в следующем виде:
                while (выражение) тело цикла

       Выражение в скобках может принимать истинное(ненулевое)  или ложное
       (нулевое) значение. Если выражение истинно, то выполняется тело цик-
       ла и выражение вычисляется снова. Если выражение ложно, то цикл while
       заканчивается.
          Пример:
             ..........................
              while ((getchar())!='r')
                    i++;
             ..........................


        Основное отличие между циклами while и do-while заключается в том,
        что тело в цикле do-while выполняется по крайней мере один раз.
        Оператор do-while формально  записывается следующим образом:
           do тело цикла while (выражение);
        Тело цикла будет выполняться до тех пор, пока выражение в скобках
        не примет ложное значение. Если оно ложно при входе в цикл, то его
        тело выполнится ровно один раз.
          Пример:
           .............................
             do
              i++;
              while (getchar()!='r');
           .............................
       Контрольные вопросы:
       1. Какие операторы организации циклов Вы знаете?
       2. Общий вид записи оператора организации циклов for?
       3. Общий вид записи оператора организации циклов while?
       4. Общий вид записи оператора организации циклов do-while?
       5. Чем отличается оператор while от do-while?
       6. Может ли тело цикла for быть заключено в () , следующие за
	  оператором for?
       7. Приведите пример цикла, организованного с помощью оператора
	  for и не имеющего тела цикла, заключенного в { }?
       8. Изобразите схематично цикл, организованный с помощью операто-
	  ра for, используя общее описание такого, цикла приведенного
	  в этой главе?
       9. Изобразите схематично цикл, организованный с помощью операто-
	  ра while, используя общее описание такого, цикла приведенного
	  в этой главе?
      10. Изобразите схематично цикл, организованный с помощью операто-
	  ра do-while, используя общее описание такого, цикла приведенного
	  в этой главе?


